{
  "name": "plexus2",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --watch packages/backend/src/index.ts",
    "test": "bun test",
    "test:backend": "bun test packages/backend",
    "typecheck": "cd packages/backend && bun tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.5"
  }
}
